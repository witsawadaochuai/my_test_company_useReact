{
  "name": "fullstack-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm install && node server.js",
    "client": "cd chat-dashboard && npm install && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "chart.js": "^4.4.9",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "highcharts": "^12.2.0",
    "highcharts-react-official": "^3.2.2",
    "jsonwebtoken": "^9.0.2"
  }
}
